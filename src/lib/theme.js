import { createTheme } from "@mui/material";

const theme = createTheme( {
    palette: {
        primary: {main: '#9cd9e4'},
        secondary: {main: '#00364d'},
        background: {default: '#f0fdff'},
    },
    typography: {
        fontFamily: 'Roboto, Arial, sans-serif',
    },
})

export default theme;